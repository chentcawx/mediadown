# Rust build artifacts (huge, regenerated by cargo)
src-tauri/target/

# Node deps
node_modules/

# Runtime output / user downloads (not source)
dist/downloads/
dist/tools/
dist/*.exe
dist/*.7z

# Bundled external tools are large binaries; shipped via dist.7z release, not git
tools/*.exe

# Root-level published artifacts (released as dist.7z instead)
MediaDown-x86.exe
MediaDown-x64.exe

# Archives
*.7z
*.zip

# OS / editor junk
.DS_Store
Thumbs.db
*.log
# Archive of old builds and temp files
archive/
